<div class="panel panel-info">
  <div class="panel-heading">
    <h3> User Management <a href="<%= add_team_url%>" class="btn btn-primary pull-right">Add Team</a></h3>

    
  </div>

  <div class="panel-body">
    <div class="row">
      <div class="col-xs-12">
        <h4>My Teams</h4>

        <% if @teams.size > 0 %>
          <% @teams.each do |team| %>


          <% end %>
        <% else %>
          No teams found.
        <% end %>
      </div>
    </div>

  </div>
</div>